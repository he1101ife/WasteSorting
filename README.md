1.在本地电脑中的环境变量配置这四个的值
dify:
  base-url: ${DIFY_BASE_URL}
  api-key: ${DIFY_API_KEY}

baidu:
  speech:
    api-key: ${BAIDU_API_KEY}
    secret-key: ${BAIDU_SECRET_KEY}
2.部署dify,选择qwen3.6模型,并在localhost/signin上运行即可
